<!DOCTYPE html>
<html>
  <head>
    <title>UbrionCom</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body>
    <div class="container">
      <% if current_user %>
        <nav>
          <ul>
            <li><%= link_to "Add new post", new_post_path %></li>
            <li><%= link_to "Desconectar", sessions_path, method: :delete%></li>
          </ul>
        </nav>
      <% end %>

      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, class: "alert alert__#{name}" %>
      <% end %>

      <%= yield %>

      <footer class="footer">
        <%= link_to "ubrion.com", root_path %>
      </footer>
    </div>
  </body>
</html>
